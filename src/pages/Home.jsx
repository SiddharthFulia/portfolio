import ScrollCinematicHero from "../components/ScrollCinematicHero";

// Home — the cinematic scroll hero is the entire landing page.
// The old "Full-Stack AI Engineer" text-hero card was removed because
// it duplicated what the cinematic hero already says. The original
// Three.js island scene was moved to /3d → "Island Fly".
const Home = () => {
  return <ScrollCinematicHero />;
};

export default Home;
