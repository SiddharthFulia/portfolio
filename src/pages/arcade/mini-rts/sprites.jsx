// MiniRts — sprite components scaffold.
//
// Canvas-rendered today, but the DOM/JSX overlays (unit selection ring,
// build menu, HUD tags) plus procedural canvas draws (grass tile,
// forest, unit body + walk-cycle frame) can be extracted here.
export {}
